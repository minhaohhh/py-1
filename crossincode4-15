#coding gbk

weight = int(input('请输入体重(公斤)：'))
height = float(input('请输入身高(米)：'))
BMI = weight / (height * height)
if BMI < 18.5:
    print('你的BMI是%.2f,体重偏轻' % BMI)
elif BMI >= 24:
    print('你的BMI是%.2f,体重偏重' % BMI)
else:
    print('你的BMI是%.2f, 体重正常' % BMI)
